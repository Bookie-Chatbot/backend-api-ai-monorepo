services:
  api-server:
    build: ./apps/api-server
    command: poetry run uvicorn src.main:app --host 0.0.0.0 --port 8000
  ai-service:
    build: ./apps/ai-service
    command: poetry run python src/main_service.py
